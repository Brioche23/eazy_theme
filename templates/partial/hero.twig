         <img src={{ Image(post.meta('hero_image')).src }} alt="boat" class="object-cover h-full w-full " />
            <div class="absolute bottom-0 w-full">
                <div class="max-w-screen-xl pb-32 md:pb-16 lg:pb-32 inside">
                    <div class="md:w-3/4">
                    {% if post.meta('hero_logo') %}
                    <img src={{Image(post.meta('hero_logo')).src | resize(1500)}} class="w-52 pb-6" alt="Logo"/>
                    {% endif %}
                        <h5 class="text-xs md:text-sm font-medium uppercase tracking-widest pb-6">
                            {{post.meta('hero_headline')}}</h5>
                        <h1 class="font-display text-2xl md:text-3xl lg:text-4xl pb-12">{{post.meta('hero_abstract')}}</h1>
                    </div>
                    {# todo mettere link #}
                    {# <div class="flex space-x-5">
                        <a href="/bunker"
                            class="bg-white hover:bg-eazy-blue text-eazy-blue hover:text-white text-sm rounded-full px-5 py-3 tracking-wide transition ease-in-out duration-300">Eazy
                            Bunker</a>
                        <a href="#"
                            class="bg-white hover:bg-eazy-blue text-eazy-blue hover:text-white text-sm rounded-full px-5 py-3 tracking-wide transition ease-in-out duration-300">Eazy
                            Green</a>
                        <a href="#"
                            class="bg-white hover:bg-eazy-blue text-eazy-blue hover:text-white text-sm rounded-full px-5 py-3 tracking-wide transition ease-in-out duration-300">Eazy
                            Yachting</a>
                    </div> #}
                </div>
            </div>